pluginManagement {
  repositories {
    maven { url "${ System.getenv('REPOSITORY_MANAGER_URL') }/ossim-deps" }
    gradlePluginPortal()
  }
}

include 'omar-video-plugin'

project(':omar-video-plugin').projectDir = "${rootProject.projectDir}/plugins/omar-video-plugin" as File
